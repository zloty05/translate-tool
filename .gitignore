# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Supabase CLI — stan lokalny (supabase/ ma też własny .gitignore)
supabase/.temp/
supabase/.branches/

# Claude Code — lokalna lista uprawnień (per maszyna)
.claude/settings.local.json
